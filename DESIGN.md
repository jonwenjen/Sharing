# Sharing 設計規範與推薦設計

這份文件記錄 Sharing 目前的設計語彙，並列出建議的改進。新增畫面或修改介面時，請先對照這份文件，讓整個 App 維持一致。

---

## 1. 設計原則

1. **手機優先、單手可用**：主要使用情境是在 LINE 裡、旅途中、一隻手拿手機。重要動作放在拇指容易按到的下半部（右下「記一筆」、底部抽屜表單）。
2. **一個主角，其他安靜**：全 App 只有一個視覺重點，就是「車票」形狀的餘額卡。其他元件都保持低調，不和它搶注意力。
3. **數字要一眼看懂**：金額是最重要的資訊。一律用等寬數字、清楚的正負顏色，並附上文字說明（「結清後你會收到」），不能只靠顏色表達。
4. **不讓人算錢**：凡是能自動算的都自動算（匯率、分攤、最少轉帳），畫面只顯示結論與必要的說明。
5. **錯誤要能補救**：刪除要確認、分享要有開關、權限不足要說明原因與下一步，不留下死路。

---

## 2. 設計語彙：「車票」

餘額卡做成一張有打孔缺口與虛線票根的車票，呼應「一起出遊、一起分帳」。

- **左側主票**：我的結算金額（大字）＋一句說明＋「你是 ○○」
- **右側票根**：總支出、紀錄筆數
- **打孔缺口**：用 CSS mask 做出上下兩個半圓，位置跟著票根寬度 `--stub` 變動

**不要**把車票元素用在其他地方（例如一般卡片也打孔），否則主角就不再特別。

---

## 3. 色彩

### 3.1 目前的色彩 Token

| Token | 淺色 | 深色 | 用途 |
|---|---|---|---|
| `--paper` | `#F2F4F7` | `#10131C` | 頁面底色 |
| `--card` | `#FFFFFF` | `#1A1F2C` | 卡片、輸入框底色 |
| `--ink` | `#18223A` | `#EEF1F7` | 主要文字 |
| `--muted` | `#5E6882` | `#9AA3B8` | 次要文字、提示 |
| `--line` | `#DDE2EB` | `#2A3142` | 分隔線、框線 |
| `--cobalt` | `#2F54EB` | `#6F8BFF` | 品牌色、選取狀態、連結 |
| `--cobalt-deep` | `#1D37B8` | `#2A45C9` | 車票底色（深） |
| `--cobalt-soft` | `#E6ECFF` | `#1E2748` | 選取中的淺底、邀請區塊 |
| `--btn` | `#2F54EB` | `#3B58E6` | 主要按鈕底色 |
| `--pos` | `#0B7A54` | `#3DD39C` | 應收、正數 |
| `--neg` | `#C22F4B` | `#FF7189` | 應付、負數、危險操作 |
| `--warn-bg` / `--warn-ink` | `#FFF4DB` / `#7A5200` | `#3A2E12` / `#FFD98A` | 警告、管理員標示 |

### 3.2 調整前的對比檢查結果（WCAG AA：一般文字需 4.5:1、大字需 3:1）

| 組合 | 對比 | 結果 |
|---|---|---|
| 主要文字 `--ink` / `--paper` | 14.3 | ✅ |
| 主要按鈕白字 / `--btn` | 5.9 | ✅ |
| 次要文字 `--muted` / `--card` | 5.0 | ✅ |
| 次要文字 `--muted` / `--paper` | **4.49** | ⚠️ 差一點點，13px 的提示文字不足 |
| 應收 `--pos` / `--card` | **4.09** | ❌ 明細裡 12px 的「你 +NT$…」不足 |
| 應付 `--neg` / `--card` | **4.47** | ⚠️ 差一點點 |
| 車票應付金額 `--ticket-neg` / 漸層亮處 `#4A6CFF` | **2.84** | ❌ 大字也不足 |
| 車票應收金額 `--ticket-pos` / 漸層亮處 | 3.25 | ✅（大字）但偏低 |
| 深色模式各組合 | 5.3 以上 | ✅ |

### 3.3 色彩調整（v1.5 已套用）

| Token | 目前 | 建議 | 調整後對比 |
|---|---|---|---|
| `--muted`（淺色） | `#667089` | `#5E6882` | 紙底 5.0、白底 5.6 |
| `--pos`（淺色） | `#0F8F63` | `#0B7A54` | 白底 5.4、紙底 4.9 |
| `--neg`（淺色） | `#D93A57` | `#C22F4B` | 白底 5.5、紙底 5.0 |
| 車票漸層起點 | `#4A6CFF` | `#2F4FE0` | 應付金額 4.2、應收 4.8 |

色相都不變，只是稍微加深，看起來幾乎一樣，但所有文字都能達到 AA。

---

## 4. 字體

| 用途 | 字體 | 說明 |
|---|---|---|
| 數字、品牌字 | Bricolage Grotesque（700／800） | 金額、標題大字，搭配 `font-variant-numeric: tabular-nums` |
| 內文（中文） | 系統字：PingFang TC → Noto Sans TC → 微軟正黑體 | 不另外下載中文字型，LINE 內開啟速度快 |

**字級**

| 層級 | 大小 | 範例 |
|---|---|---|
| 車票金額 | 34–46px（依寬度自動縮放） | 結清後你會收到 **NT$5,124** |
| 頁面大標 | 40px | 首頁「帳本」 |
| 統計總額 | 34px | 團體總支出 |
| 區塊標題 | 14px 粗體、`--muted` | 分類、每個人 |
| 內文 | 15–16px | 項目名稱、表單 |
| 提示 | 12.5–13px、`--muted` | 「小安 先付，4 人分攤」 |

**規則**：金額太長時用 `fitText()` 自動縮小字級，絕不換行。

---

## 5. 間距、圓角、陰影

- **間距**：以 4 的倍數為主（4、8、10、12、14、16、20）。頁面左右留白 16–20px。
- **圓角**：`--r-lg` 22px（車票、帳本卡、大輸入區）、`--r-md` 14px（卡片、清單列）、`--r-sm` 10px（輸入框）、膠囊 99px（按鈕、chip、分頁）。
- **陰影**：幾乎不用。只有浮動按鈕「記一筆」有陰影，讓它浮在內容上。層次靠底色區分（`--paper` 上放 `--card`），不靠陰影。

---

## 6. 版面結構

```
┌──────────────────────────────┐
│ ←   帳本名稱        ?   ⚙     │  頂部（毛玻璃、固定）
├──────────────────────────────┤
│ 👥 群組名稱  建立者 ○○        │  帳本資訊
│ ╭────────────────┬────────╮  │
│ │ 結清後你會收到    │ 總支出  │  車票（唯一主角）
│ │ NT$5,124        │ NT$38k │
│ ╰────────────────┴────────╯  │
│ [明細] 結算  統計  成員        │  分頁（捲動時黏在頂部）
│ ……內容……                      │
│                    [+ 記一筆] │  浮動按鈕（只在明細頁）
└──────────────────────────────┘
```

- 內容最大寬度 560px，在平板或電腦上置中。
- 表單一律用**底部抽屜**（sheet），不跳新頁面，方便單手操作也不會迷路。
- 抽屜高度：簡單表單自動高度、記帳編輯器固定 92%。

---

## 7. 元件

| 元件 | 規格 | 使用時機 |
|---|---|---|
| 主要按鈕 `.btn.primary` | 高 48、膠囊、`--btn` 底白字 | 每個畫面最多一個，例如「記下這筆」 |
| 次要按鈕 `.btn.ghost` | 高 48、白底框線 | 其他動作 |
| 危險按鈕 `.btn.text-danger` | 無底色、紅字 | 刪除、移除，一律搭配確認框 |
| 分段控制 `.seg` | 高 44（小尺寸 40）、灰底膠囊、選中白底 | 2–3 個互斥選項（平分／自訂金額／依份數） |
| Chip `.chip` | 高 44、膠囊、選中變品牌淺底 | 分類、付款人等快速選擇，可多排換行 |
| 開關 `.switch` | 50×30 | 是否分享、是否啟用公費 |
| 清單列 `.rec` / `.member` | 圖示＋兩行文字＋右側金額或箭頭 | 明細、成員 |
| 提示 `.toast` | 畫面頂端、2.4 秒消失 | 操作結果；錯誤用紅底 |
| 抽屜 `sheet()` | 可設 `required`（不能關閉） | 表單、確認、身分選擇 |
| 標籤 `.tag` | 11px、膠囊 | 我建立的、管理員檢視、已封存 |

---

## 8. 互動模式

- **必要步驟**：還不是成員的人進入帳本時，用不能關閉的抽屜要求先選身分，並提供「先回帳本列表」的出口。
- **確認**：刪除、移除成員、重設邀請連結都要確認，並說明後果（例如「舊連結會立刻失效」）。
- **分享**：每次新增／修改／刪除都有「分享到 LINE 群組」開關，下方寫出目標群組名稱；沒有連結群組時開關停用並說明原因。
- **錯誤**：錯誤訊息要說明「發生什麼」與「下一步」，例如「你沒有這本帳本的權限，請向帳本成員索取邀請連結」。
- **載入**：開啟帳本時先顯示骨架畫面（車票與明細的灰色佔位），其他操作用頂端細進度條。
- **復原**：刪除後提示列會出現「復原」按鈕 5 秒。

---

## 9. 文案語氣

- 繁體中文、口語、簡短，像朋友在說話：「誰先付的」「誰要分攤」「結清後你會收到」。
- 按鈕用動詞開頭：「記下這筆」「記為已付款」「分享到 LINE」。
- 金額格式：幣別符號＋千分位，例如 `NT$1,200`、`¥3,000`、`US$12.50`；台幣、日圓、韓圓不顯示小數。
- 正負號：應收顯示 `+`，應付顯示 `-`，同時搭配文字，不只靠顏色。

---

## 10. 無障礙

- 所有圖示按鈕都要有 `aria-label`（返回、設定、說明、複製）。
- 分頁用 `role="tab"` 與 `aria-selected`；選項按鈕用 `aria-pressed`。
- 抽屜開啟時焦點移到第一個輸入框，關閉後回到原本的按鈕。
- 支援 `prefers-reduced-motion`：關閉所有轉場動畫。
- 支援深色模式：跟隨系統設定。

---

## 11. 推薦設計改進

依影響程度排序。**v1.5 已全部實作**，以下保留作為設計決策紀錄；例外：C1 決定維持 emoji，D1 需在 iPhone 與 Android 實機確認。

### A. 可讀性與易用性（建議優先）

| # | 建議 | 原因 | 做法 |
|---|---|---|---|
| A1 | 調整 4 個顏色（見 3.3） | 部分文字對比不足，戶外陽光下更難看清 | 只改 CSS 變數，約 5 分鐘 |
| A2 | 觸控目標加大到 44px | chip、小按鈕、分段控制目前 32–38px，手指容易點錯 | `.chip`、`.btn.sm`、`.seg button` 最小高度改 44px；`.seg.sm` 改 40px |
| A3 | 記帳編輯器分成「必填」與「更多」 | 表單很長，常用的只有金額、項目、誰付、誰分 | 日期、備註、分享開關收進「更多選項」摺疊區，預設收合（今天日期、分享開啟） |
| A4 | 刪除後提供「復原」 | 誤刪時只能重新輸入 | 刪除後 toast 顯示「已刪除 · 復原」5 秒；後端已是軟刪除，只需加一個還原 API |

### B. 資訊呈現

| # | 建議 | 原因 | 做法 |
|---|---|---|---|
| B1 | 首頁帳本卡顯示「我的餘額」 | 不用點進去就知道要收還是要付 | 卡片右側顯示 `+NT$320` 或 `-NT$1,200`（`/api/me/ledgers` 多回傳一個欄位） |
| B2 | 結算頁把「跟我有關的轉帳」放最上面 | 大家最關心自己要付誰、誰要付我 | 依「我要付 → 別人付我 → 其他人」排序，並加小標題分組 |
| B3 | 載入時顯示骨架畫面 | 目前只有頂端細線，打開帳本時畫面會空白一下 | 車票與三列明細的灰色佔位塊 |
| B4 | 明細可篩選 | 紀錄一多就很難找 | 明細上方加 chip 篩選：全部／我付的／跟我有關／各分類 |
| B5 | 統計圖可點 | 看到某天花很多，想知道花在哪 | 點長條或分類 → 切到明細並套用篩選 |

### C. 一致性與質感

| # | 建議 | 原因 | 做法 |
|---|---|---|---|
| C1 | 分類圖示統一風格 | emoji 在 iOS、Android、Windows 長得不一樣 | 維持 emoji（最省事），或改用同一套線條 SVG 圖示 |
| C2 | 空狀態加上簡單插圖 | 新帳本、沒有紀錄時畫面很空 | 用一張小車票的線條插圖＋一句引導文字 |
| C3 | 記一筆成功時的回饋 | 目前只有 toast | 新增的那筆在明細中短暫亮一下（背景淡藍 1 秒） |
| C4 | 首頁加「最近使用」排序 | 帳本多時要找很久 | 依最後開啟時間排序，封存的放最後 |

### D. LINE 環境

| # | 建議 | 原因 | 做法 |
|---|---|---|---|
| D1 | 預留 LINE 內建瀏覽器的上方工具列空間 | 部分機型上方標題列會蓋到頂部 | 已使用 `safe-area-inset`，建議在 iPhone 與 Android 實機各確認一次 |
| D2 | 機器人回覆卡片與網頁視覺一致 | 目前 Flex 卡片是藍色文字，和網頁車票風格不同 | 卡片頂部加一條品牌色、金額用粗體大字，跟車票呼應 |

---

## 12. 做與不做

| ✅ 做 | ❌ 不做 |
|---|---|
| 用 CSS 變數取色，深色模式自動適用 | 在元件裡寫死顏色 |
| 金額一律等寬數字、不換行 | 讓金額斷成兩行 |
| 一個畫面只有一個主要按鈕 | 多個藍色大按鈕並排 |
| 正負同時用顏色＋符號＋文字 | 只靠紅綠色區分 |
| 表單用底部抽屜 | 跳到新頁面填表 |
| 危險操作先確認並說明後果 | 按一下直接刪除 |
