# 驗證報告（QA 驗證員）

驗證環境：Node 22（node:sqlite 模擬 D1）、Chromium／Playwright，iPhone 尺寸 390×844 @2x，淺色＋深色。

## 結果總覽
| 類別 | 項目數 | 結果 |
|---|---|---|
| 單元測試（分攤、匯率、公費、驗證、格式） | 9 | ✅ 全數通過 |
| 演算法測試（含 600 組隨機案例與暴力解比對、18 人效能） | 5 | ✅ 全數通過 |
| API 整合測試（權限、認領、封存、公費、推播、Webhook 簽章與「結算」回覆） | 11 | ✅ 全數通過 |
| 手機 E2E 完整流程（12 張截圖審查） | 1 條流程、20+ 斷言 | ✅ 通過，無 console 錯誤 |

## 驗證中發現並已修正的問題
| # | 問題 | 嚴重度 | 修正 |
|---|---|---|---|
| 1 | 記帳表單內容被陣列字串化，欄位消失（`replaceChildren` 不會攤平陣列） | 高 | 新增 `mount()`，統一攤平並略過 null |
| 2 | 首頁出現 "null" 文字 | 中 | 同上 |
| 3 | 表單在手機上可水平捲動，日期／備註欄被截斷 | 中 | 抽屜禁止水平溢出；grid 子項 `min-width:0` |
| 4 | 餘額大數字斷成兩行（NT$5,12 / 4） | 中 | 改為不換行＋`fitText()` 自動縮字 |
| 5 | 「加入」按鈕在窄欄被擠成直排 | 低 | 按鈕 `white-space:nowrap; flex:none` |
| 6 | 提示訊息蓋住主要按鈕 | 低 | 移到畫面頂端 |
| 7 | 深色模式主按鈕對比不足 | 低 | 深色改用較深的按鈕色 |
| 8 | 「記一筆」浮動按鈕遮住結算頁的「記為已付款」 | 低 | 浮動按鈕只在「明細」分頁顯示 |
| 9 | 刪除成員 SQL 混用 `?` 與 `?1` 參數 | 中 | 統一為編號參數，已加測試 |

## 已知限制（交付時告知）
- 在 LINE 內建瀏覽器中，CSV 下載可能被擋；已在畫面提示改用外部瀏覽器開啟。
- 機器人推播（從外部瀏覽器記帳時）會消耗官方帳號每月免費訊息額度；在群組內開啟則不消耗。
- LIFF 在正式環境的登入、`sendMessages`、`shareTargetPicker` 需在實機 LINE 上驗證（本環境無 LINE 帳號），已以示範模式與模擬測試覆蓋其餘邏輯。
